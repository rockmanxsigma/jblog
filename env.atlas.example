# Configuration MongoDB Atlas
# Remplacez les valeurs par vos identifiants MongoDB Atlas
MONGODB_ATLAS_URI=mongodb+srv://<username>:<password>@<cluster-url>/<database>?retryWrites=true&w=majority

# Configuration MongoDB locale (fallback)
MONGODB_URI=mongodb://localhost:27017/jgazette

# Configuration JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Configuration CORS
CORS_ORIGIN=http://localhost:80

# Configuration Uploads
UPLOAD_PATH=./uploads

# Configuration pour la production (optionnel)
# NODE_ENV=production
# PORT=5000

# Instructions pour MongoDB Atlas :
# 1. Connectez-vous à MongoDB Atlas (https://cloud.mongodb.com)
# 2. Créez un cluster (gratuit M0 disponible)
# 3. Créez un utilisateur de base de données
# 4. Configurez l'accès réseau (ajoutez votre IP ou 0.0.0.0/0 pour toutes les IPs)
# 5. Obtenez la chaîne de connexion dans "Connect" > "Connect your application"
# 6. Remplacez <username>, <password>, <cluster-url>, et <database> dans MONGODB_ATLAS_URI
